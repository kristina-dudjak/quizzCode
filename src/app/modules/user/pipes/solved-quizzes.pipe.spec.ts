import { SolvedQuizzesPipe } from './solved-quizzes.pipe';

describe('SolvedQuizzesPipe', () => {
  it('create an instance', () => {
    const pipe = new SolvedQuizzesPipe();
    expect(pipe).toBeTruthy();
  });
});
